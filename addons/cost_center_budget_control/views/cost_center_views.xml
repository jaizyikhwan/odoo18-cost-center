<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Cost Center Form View -->
    <record id="view_cost_center_form" model="ir.ui.view">
        <field name="name">cost.center.form</field>
        <field name="model">cost.center</field>
        <field name="arch" type="xml">
            <form string="Cost Center">
                <header>
                    <field name="active" widget="boolean_button" options='{"term": "Archive", "active_text": "Active"}'/>
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="action_view_budget_plans" type="object"
                                class="oe_stat_button" icon="fa-pie-chart">
                            <field name="budget_plan_count" widget="statinfo"
                                   string="Budget Plans"/>
                        </button>
                        <button name="action_view_budget_plans" type="object"
                                class="oe_stat_button" icon="fa-money"
                                invisible="budget_plan_count == 0">
                            <field name="budget_plan_total_planned" widget="statinfo"
                                   string="Total Planned"/>
                        </button>
                        <button name="action_view_over_budget_lines" type="object"
                                class="oe_stat_button text-danger" icon="fa-exclamation-triangle"
                                invisible="over_budget_line_count == 0">
                            <field name="over_budget_line_count" widget="statinfo"
                                   string="Over Budget"/>
                        </button>
                    </div>
                    <field name="company_currency_id" invisible="1"/>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="code"/>
                            <field name="parent_id" options="{'no_create': True, 'no_open': True}"/>
                        </group>
                        <group>
                            <field name="company_id" options="{'no_create': True, 'no_open': True}"/>
                            <field name="analytic_account_id" domain="[('company_id', '=', company_id)]" options="{'no_create': True, 'no_open': True}"/>
                            <field name="responsible_id" options="{'no_create': True, 'no_open': True}"/>
                        </group>
                    </group>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids" widget="mail_followers"/>
                    <field name="activity_ids" widget="mail_activity"/>
                    <field name="message_ids" widget="mail_thread"/>
                </div>
            </form>
        </field>
    </record>

    <!-- Cost Center List View -->
    <record id="view_cost_center_list" model="ir.ui.view">
        <field name="name">cost.center.list</field>
        <field name="model">cost.center</field>
        <field name="arch" type="xml">
            <list string="Cost Centers">
                <field name="code"/>
                <field name="name"/>
                <field name="parent_id"/>
                <field name="company_id"/>
                <field name="active"/>
            </list>
        </field>
    </record>

    <!-- Cost Center Search View -->
    <record id="view_cost_center_search" model="ir.ui.view">
        <field name="name">cost.center.search</field>
        <field name="model">cost.center</field>
        <field name="arch" type="xml">
            <search string="Cost Center Search">
                <field name="name" string="Name"/>
                <field name="code" string="Code"/>
                <filter string="Archived" name="inactive" domain="[('active', '=', False)]"/>
                <filter string="Company" name="company_filter" domain="[('company_id', '=', company_id)]"/>
                <group expand="0" string="Group By">
                    <filter string="Company" name="group_company" context="{'group_by': 'company_id'}"/>
                    <filter string="Parent" name="group_parent" context="{'group_by': 'parent_id'}"/>
                </group>
            </search>
        </field>
    </record>

</odoo>
